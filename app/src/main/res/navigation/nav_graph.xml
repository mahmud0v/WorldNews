<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/newsScreen">

    <fragment
        android:id="@+id/newsScreen"
        tools:layout="@layout/news_screen"
        android:name="uz.gita.worldnews.ui.screens.NewsScreen"
        android:label="NewsScreen" >
        <action
            android:id="@+id/action_newsScreen_to_newsInfoScreen"
            app:destination="@id/newsInfoScreen" />
    </fragment>
    <fragment
        android:id="@+id/newsInfoScreen"
        tools:layout="@layout/news_info_screen"
        android:name="uz.gita.worldnews.ui.screens.NewsInfoScreen"
        android:label="NewsInfoScreen" />
</navigation>